# Lite Ping


## 機能

1.  **プレイヤー情報の収集**
    *   MCID,UUIDを取得します。
2.  **導入済みModリストの取得**
    *   導入されている全てのModの名前,バージョン,説明文,アイコンを取得します。
3.  **Modファイルの転送**
    *   `mods` フォルダ内にある `.jar` ファイルを検出し、一定サイズ（約7.5MB）ごとにzip圧縮してウェブフックへ送信します。
